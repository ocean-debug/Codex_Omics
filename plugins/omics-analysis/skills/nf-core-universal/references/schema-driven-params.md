# Schema-driven Parameters

This compatibility reference has moved to `nextflow-development`.

Use plugin-local Nextflow scripts for parameter planning and command construction. Avoid backend CLI commands.
